<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.opensourcebridge.navigator"
    android:versionCode="15"
    android:versionName="2013.0" >

    <application
        android:description="@string/app_description"
        android:icon="@drawable/hugelogo"
        android:label="OSBridge" >
        <activity
            android:name="org.opensourcebridge.navigator.Main"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.opensourcebridge.navigator.Schedule"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk android:minSdkVersion="3" />

</manifest>